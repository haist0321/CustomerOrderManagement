<Window x:Class="CustomerOrderManagement.Views.EditOrderWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Sửa đơn hàng"
        Height="300"
        Width="400">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Label Content="Tổng tiền:" />
        <TextBox x:Name="txtTotal"
                 Grid.Row="0"
                 Margin="0,25,0,0" />

        <Label Grid.Row="1"
               Content="Giảm giá:"
               Margin="0,10,0,0" />
        <TextBox x:Name="txtDiscount"
                 Grid.Row="1"
                 Margin="0,30,0,0" />

        <Button Content="Lưu thay đổi"
                Grid.Row="2"
                Margin="0,20,0,0"
                Click="Save_Click" />
    </Grid>
</Window>
